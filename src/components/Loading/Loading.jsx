import Container from "../Container/Container";

const Loading = () => {
    return (
        <div className="loading-inner col-span-12 text-center">
            <span className="loading loading-spinner loading-xl"></span>
        </div>
    );
};

export default Loading;